# Week 4 Backend API

## REST Endpoints

* GET /api/tasks
* GET /api/tasks/:id
* POST /api/tasks
* PUT /api/tasks/:id
* DELETE /api/tasks/:id
* GET /api/search?q=keyword

This project contains 6 REST API endpoints with CRUD operations and search functionality.
